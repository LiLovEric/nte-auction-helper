# 异环拍卖会分析工具启动说明

## 推荐启动方式
1. 将整个 `desktop_app` 文件夹完整解压到本地。
2. 双击 `启动工具.pyw`。
3. 首次启动时会自动检查依赖并尝试安装，完成后会自动打开程序窗口。

## 启动逻辑
- 启动器会先检查 `flask`、`numpy`、`pywebview`、`pillow`、`rapidocr-onnxruntime`、`werkzeug` 是否可用。
- 如果缺少依赖，会按以下顺序尝试安装：
  - 本地 `wheels/` 目录
  - 清华镜像源
  - 官方 PyPI
- 安装成功后会写入 `.deps_ready`，方便下次快速启动。
- 如果安装失败，会生成 `startup_error.log`，并弹出错误提示。

## 备用启动方式
如果需要手动启动，也可以在 `desktop_app` 目录中执行：

```bash
python desktop_app.py
```

如果系统使用 `py` 启动器，也可以执行：

```bash
py -3 desktop_app.py
```

## 离线或弱网环境
- 如果电脑没有网络，建议提前把依赖的 wheel 包放进 `wheels/` 目录。
- 如果没有 `wheels/`，启动器仍会尝试联网安装，但弱网环境下可能失败。
- 失败后请优先查看 `startup_error.log`，里面会记录 pip 的具体输出。

## 程序文件说明
- `red_history.json` 用于保存历史数据。
- `red_weights.json` 用于保存红色权重配置。
- 这两个文件会和程序放在同一目录下。

## 常见问题
- 如果提示缺少模块，重新运行 `启动工具.pyw` 即可。
- 如果仍然失败，检查是否网络受限，或者是否缺少 `wheels/` 离线依赖包。
- 如果是第一次运行，启动会比平时慢一些，因为需要先准备依赖。
